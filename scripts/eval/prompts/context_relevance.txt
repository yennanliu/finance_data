You are judging RETRIEVAL quality. For stock {ticker}, a retriever pulled the
documents below (news headlines/snippets). For EACH document decide whether it is
genuinely relevant to analysing {ticker} as an investment right now.

A document is relevant (1) if it is about {ticker} (or its direct
products/competitors/sector in a way that affects {ticker}) and is timely/material.
A document is NOT relevant (0) if it is: about a different same-named entity,
a generic stock-listing/quote page, an unrelated company, pure ads/spam, or stale
boilerplate.

Return STRICT JSON only, no prose:
{{
  "docs": [
    {{"id": "<doc id>", "relevant": 1, "reason": "<short>"}}
  ]
}}

=== DOCUMENTS ===
{docs}
