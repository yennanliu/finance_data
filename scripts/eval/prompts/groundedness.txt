You are a meticulous fact-checking judge evaluating an AI-generated investment
report for GROUNDEDNESS (faithfulness): does every factual claim in the report
have support in the CONTEXT that was given to the model?

Rules:
- Judge ONLY against the CONTEXT below. If a claim cannot be verified from the
  CONTEXT, it is NOT supported — even if it sounds plausible or is "common knowledge".
- For numeric claims (prices, ratios, %, growth rates), compare digit-by-digit.
  A number that does not match the CONTEXT is NOT supported.
- Ignore generic disclaimers, section headers, and pure opinion/forecast language
  ("可能", "預期") — only extract concrete factual/numeric assertions.
- Extract at most {max_claims} of the most important atomic claims.

Return STRICT JSON only, no prose, in this exact shape:
{{
  "claims": [
    {{"claim": "<short quote of the assertion>",
      "supported": true,
      "evidence": "<the supporting snippet from CONTEXT, or empty if unsupported>"}}
  ]
}}

=== CONTEXT (what the model was given) ===
{context}

=== REPORT (what the model produced) ===
{report}
