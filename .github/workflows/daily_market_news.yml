name: Daily Market News

# ── Schedules (UTC → UTC+8) ──────────────────────────────────────────────────
#   21:00 → 05:00  TSLA
#   21:24 → 05:24  PLTR
#   21:48 → 05:48  ONDS
#   22:12 → 06:12  TSM
#   22:36 → 06:36  GOOG
#   23:00 → 07:00  AMZN
#   23:24 → 07:24  SOFI
#   23:48 → 07:48  AMD
#   00:12 → 08:12  MSFT
#   00:36 → 08:36  RKLB
on:
  schedule:
    - cron: "0 21 * * *"    # TSLA
    - cron: "24 21 * * *"   # PLTR
    - cron: "48 21 * * *"   # ONDS
    - cron: "12 22 * * *"   # TSM
    - cron: "36 22 * * *"   # GOOG
    - cron: "0 23 * * *"    # AMZN
    - cron: "24 23 * * *"   # SOFI
    - cron: "48 23 * * *"   # AMD
    - cron: "12 0 * * *"    # MSFT
    - cron: "36 0 * * *"    # RKLB

  workflow_dispatch:
    inputs:
      ticker:
        description: "Stock ticker symbol (e.g. AAPL, TSLA, NVDA, SPY)"
        required: false
        default: "TSLA"
      model:
        description: "Claude model ID"
        required: false
        default: "claude-sonnet-4-6"
        type: choice
        options:
          - claude-sonnet-4-6
          - claude-opus-4-6
          - claude-haiku-4-5-20251001
      max_tokens:
        description: "Max output tokens"
        required: false
        default: "20000"

permissions:
  contents: write   # needed to commit & push generated report

concurrency:
  group: "daily-market-news-${{ github.run_id }}"
  cancel-in-progress: false

jobs:
  fetch-news:
    name: Fetch & Generate News Report
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── 2. Python ─────────────────────────────────────────────────
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # ── 3. Dependencies ───────────────────────────────────────────
      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install anthropic>=0.40.0 yfinance>=0.2.40

      # ── 4. Resolve parameters ─────────────────────────────────────
      #   workflow_dispatch → use user inputs
      #   scheduled crons   → map to pre-configured ticker
      - name: Resolve parameters
        id: params
        run: |
          EVENT="${{ github.event_name }}"
          SCHEDULE="${{ github.event.schedule }}"

          if [ "$EVENT" = "workflow_dispatch" ]; then
            TICKER="${{ github.event.inputs.ticker }}"
            MODEL="${{ github.event.inputs.model }}"
            MAX_TOKENS="${{ github.event.inputs.max_tokens }}"
          elif [ "$SCHEDULE" = "0 21 * * *" ]; then
            TICKER="TSLA"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "24 21 * * *" ]; then
            TICKER="PLTR"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "48 21 * * *" ]; then
            TICKER="ONDS"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "12 22 * * *" ]; then
            TICKER="TSM"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "36 22 * * *" ]; then
            TICKER="GOOG"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "0 23 * * *" ]; then
            TICKER="AMZN"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "24 23 * * *" ]; then
            TICKER="SOFI"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "48 23 * * *" ]; then
            TICKER="AMD"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "12 0 * * *" ]; then
            TICKER="MSFT"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          elif [ "$SCHEDULE" = "36 0 * * *" ]; then
            TICKER="RKLB"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          else
            # Fallback
            TICKER="TSLA"
            MODEL="claude-sonnet-4-6"
            MAX_TOKENS="20000"
          fi

          # Apply defaults for empty values
          TICKER="${TICKER:-TSLA}"
          MODEL="${MODEL:-claude-sonnet-4-6}"
          MAX_TOKENS="${MAX_TOKENS:-20000}"

          TICKER_UPPER=$(echo "$TICKER" | tr '[:lower:]' '[:upper:]')
          TICKER_LOWER=$(echo "$TICKER" | tr '[:upper:]' '[:lower:]')
          DATE_TIME=$(date -u +"%Y-%m-%d")
          OUTPUT_DIR="claude_code/market_news/${TICKER_LOWER}/${DATE_TIME}"

          echo "ticker=${TICKER_UPPER}"      >> "$GITHUB_OUTPUT"
          echo "model=${MODEL}"              >> "$GITHUB_OUTPUT"
          echo "max_tokens=${MAX_TOKENS}"    >> "$GITHUB_OUTPUT"
          echo "output_dir=${OUTPUT_DIR}"    >> "$GITHUB_OUTPUT"

          echo "=== Parameters ==="
          echo "  Event:      ${EVENT}"
          echo "  Schedule:   ${SCHEDULE:-n/a}"
          echo "  Ticker:     ${TICKER_UPPER}"
          echo "  Model:      ${MODEL}"
          echo "  Max tokens: ${MAX_TOKENS}"
          echo "  Output dir: ${OUTPUT_DIR}"

      # ── 5. Verify API key secret is set ──────────────────────────
      - name: Check ANTHROPIC_API_KEY secret
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ -z "$ANTHROPIC_API_KEY" ]; then
            echo "::error::ANTHROPIC_API_KEY secret is not set."
            echo "Go to: Settings → Secrets and variables → Actions → New repository secret"
            echo "Name: ANTHROPIC_API_KEY  |  Value: your Anthropic API key"
            exit 1
          fi
          echo "✅ ANTHROPIC_API_KEY is set."

      # ── 6. Generate news report ───────────────────────────────────
      - name: Generate market news report
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python scripts/generate_market_news.py \
            "${{ steps.params.outputs.ticker }}" \
            --output-dir  "${{ steps.params.outputs.output_dir }}" \
            --model       "${{ steps.params.outputs.model }}" \
            --max-tokens  "${{ steps.params.outputs.max_tokens }}"

      # ── 7. Commit & push ──────────────────────────────────────────
      - name: Commit and push report
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add "${{ steps.params.outputs.output_dir }}/"

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            DATE=$(date -u +"%Y-%m-%d")
            TICKER="${{ steps.params.outputs.ticker }}"
            git commit -m "chore: auto-fetch ${TICKER} market-news ${DATE}"
            git push
            echo "✅ News report committed and pushed."
          fi
